<%- include("templates/header") %> 
<h1>You are on the protected page, <%= userName %></h1>
    <div class="row">
        <% let i=0; %>
            <% for(i=1; i <= 3; i++) { %>
                <%- include("templates/image", {imageName: `00${i}.jpg`}) %>
            <% } %>
    </div>
<%- include("templates/footer") %>